This directory contains configurations scripts for a c/c++ project that uses
conan and cmake.

You can run this scripts manually **ONLY from the main directory** 
(`python3 ./scripts/...`), or execute the appropiate VSCode taks.